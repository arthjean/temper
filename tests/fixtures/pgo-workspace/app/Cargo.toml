[package]
name = "pgo-workspace-app"
version = "0.1.0"
edition = "2024"
publish = false
build = "build.rs"

[dependencies]
pgo-workspace-support = { path = "../support" }
temper-macro = { path = "../temper-macro" }

