[package]
name = "temper-units-app"
version = "0.1.0"
edition = "2024"
publish = false
build = "build.rs"

[dependencies]
temper-units-lib = { path = "../unitlib" }
temper-units-macro = { path = "../unitmacro" }
